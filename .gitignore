# dependencies
node_modules/
npm-debug.log*

# build output
dist/

# env / secrets (this server needs none, but never commit any)
.env
.env.*
!.env.example
*.pem
*.key
*secret*
*credential*

# local-only ops docs (production/infra specifics — never publish to this repo)
local/
*.local.md

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/settings.json
